{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "Default": ""
  },
  "Authentication": {
    "Oidc": {
      "_comment": "BME IdP (Shibboleth). ClientSecret is NEVER stored here — supply it via the Authentication__Oidc__ClientSecret environment variable (or user-secrets locally).",
      "Authority": "https://idp.bme.hu",
      "ClientId": "AUTAhkClient",
      "ClientSecret": "",
      "Scopes": [ "openid", "email", "userinfo" ],
      "UsePkce": false,
      "ResponseMode": "query",
      "EndSessionEndpoint": ""
    }
  }
}
